package com.ruskonert.GameClient.connect;

import com.ruskonert.GameClient.connect.server.ServerConnection;
import com.ruskonert.GameEngine.property.ServerProperty;
import com.ruskonert.GameEngine.server.Server;

public class GameConnection
{
    public static Server getServerConnection()
    {
        //ServerConnection connection = new ServerConnection(ServerProperty.CLIENT_SERVER_REQUEST);
        return null;
    }
}
